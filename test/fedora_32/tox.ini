[tox]
envlist = py37

[testenv]
whitelist_externals = docker
deps = -rrequirements.txt
commands = docker build -f test/fedora_32.Dockerfile -t pytest_pihole:fedora_32 ../../
           pytest {posargs:-vv -n auto} ./test/
